{
    "version": "0.2.0",
    "configurations": [
      {
        "name": "Start",
        "type": "go",
        "request": "launch",
        "mode": "auto",
        "envFile": "${workspaceFolder}/.vscode/.env",
        "program": "${workspaceFolder}/cmd",
        "cwd": "${workspaceFolder}"
      }
    ]
  }